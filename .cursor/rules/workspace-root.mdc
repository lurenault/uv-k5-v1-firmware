---
description: 将 k5-v4 视为唯一工作区根；路径、构建与改动范围均相对本目录
alwaysApply: true
---

# 工作区根目录：k5-v4

本规则在 **以 `k5-v4` 为 Cursor 工作区根**（「打开文件夹」指向本目录）时生效。

## 对助手的约定

1. **范围**：只针对本固件树（`k5-v4`）。不要改上级目录 `uv-k1-k5v3-firmware-custom` 下的 `k5-v1` / `k5-v2` / `k5-v3` 等，除非用户明确要求跨目录操作。
2. **路径**：一律相对仓库根（即本目录）。例如 `Makefile`、`compile-with-docker.sh`、`app/`、`driver/`、`ui/`。
3. **构建**：以本目录下 `compile-with-docker.sh` 与 `Dockerfile` 为准；`docker build` 上下文为 `.`，产物在 `compiled-firmware/`（若脚本有挂载）。**勿**在助手所在环境假设已安装交叉工具链并执行裸机 `make`；需要验证编译时以 Docker 脚本或 CI（本仓库 GitHub Actions）为准。
4. **CI**：若需提交流水线，以本仓库（`k5-v4`）配置的 remote / Actions 为准；与外层 monorepo 无自动关联。
5. **用语与编译场景（用户口头指令）**：
   - 用户说 **Docker** / **docker**：一律理解为 **本地编译**——在用户本机用 Docker（如 `compile-with-docker.sh`、`Dockerfile`），**不要**当成「在 Cursor/云端代跑 Docker」除非用户明确要助手在其环境执行。
   - 用户说 **GitHub** / **Actions** / **action**（及「CI」若明显指 GitHub）：一律理解为 **远端编译**——以本仓库 **GitHub Actions**（`.github/workflows/`）为准，通过 push / 运行 workflow 在 GitHub 上构建与出 artifact。
6. **GitHub Actions 与自动 Release（约定 + 已实现于 workflow）**：
   - 当 **`main` 上 workflow 构建成功** 时，由 `.github/workflows/main.yml` **自动** 创建/刷新一个 GitHub Release：标签形如 `auto-<7位短SHA>`。Release 上 **只挂一个 `.bin` 资产**（用户口语「裸 bin」指 **不再套一层 zip/artifact 包**，直接下 `.bin`），其 **内容仍是 packed**（与 `firmware.packed.bin` / `fw-pack.py` 一致），文件名为 **`bg7nzl-k5v1-<短SHA>.bin`**（与 Artifact 里那份同名文件同源）。
   - **Release 正文**来自仓库根目录 **[`CurrentReleaseNotes.md`](CurrentReleaseNotes.md)**（英文 **叙述性说明**，**不要**写成 raw `git log` 列表）。CI **只读取该文件**，不在 workflow 里拼长文。手动打的 **`v*` tag** 仍走原有逻辑，与上述自动 release 并存。
7. **每次 `commit` 且 `push` 到 `main` 之前（助手必做）**：
   1. 用 GitHub API 查询 **`releases/latest`** 的 **`target_commitish`**（上一版 release 对应的 commit SHA）。
   2. 若成功：根据 **`git log <该SHA>..HEAD`**、**`git diff <该SHA>..HEAD --stat`**（及必要时的文件 diff）用 **英文** 写 **概括性更新说明**，**覆盖写入** [`CurrentReleaseNotes.md`](CurrentReleaseNotes.md)，并把该文件 **纳入本次暂存区**。
   3. 若查询失败（404、无网络等）：则根据 **本地尚未 push 的提交**（**`git log origin/main..HEAD`**）以及 **已暂存未提交变更**（**`git diff --cached --stat`**）用英文写说明并覆盖 `CurrentReleaseNotes.md`（涵盖所有因未 push 而未触发过 Action 的累积变更），并纳入暂存区。
   - 然后再执行 `git commit` / `git push`（与用户约定一致时）。

## 使用方式（给人看的）

在 Cursor 中选择 **文件 → 打开文件夹 → 选 `k5-v4`**，即可让规则与路径语义与本约定一致。
